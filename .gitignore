# Local secrets (Vite loads project-root `.env.local` as a file, not a folder)
.env
.env.local
.env.*.local

node_modules
dist

# Python cache
__pycache__/
*.pyc
*.pyo

# Data files (sensitive business data)
*.xlsx
*.xls
data-migration/csv/
